package team.a501.rif.controller;

import org.springframework.web.bind.annotation.RestControllerAdvice;

@RestControllerAdvice
public class RifGlobalExceptionHandler {

    // 여기서 모든 RuntimeException을 다 처리하도록 한다
}
